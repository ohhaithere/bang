server:
  port: 8021

spring:
  application:
    name: gateway

zuul:
  routes:

eureka:
  client:
    serviceUrl:
      defaultZone: http://registry:8761/eureka/
  instance:
      hostname: registry
      nonSecurePort: 8761